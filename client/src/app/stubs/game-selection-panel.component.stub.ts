import { Component, Input } from '@angular/core';

@Component({
    selector: 'app-game-selection-panel',
    template: '',
})
export class GameSelectionPanelStubComponent {
    @Input() isConfig: boolean = false;
}
